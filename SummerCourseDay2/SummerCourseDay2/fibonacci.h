#include <stdio.h>
#include <map>
#include <iterator>
using namespace std;
int fibonacci(int a);
int optfib(int a, map<int ,int> m);
void testFib();
void findFibLogN(int n);
int **fibMatrix(int **arr, int n);
int** multiplyMatrix(int **matrix1, int **matrix2);